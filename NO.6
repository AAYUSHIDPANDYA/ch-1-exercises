//Verify that the expression getchar() != EOF is 0 or 1. 
#include <stdio.h>

int main()
{
  printf("PRESS ANY CHARACTER AND THEN PRESS ENTER \n");
  printf(" getchar() != EOF is %d\n", getchar() != EOF);
  return 0;
}
